[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "spyex"
version = "0.1.0"
description = "Public profile scraper for follower, friend, and fan counts"
readme = "README.md"
authors = [{name = "Spyex Maintainers"}]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
spyex-scrape = "scraper.cli:main"
